'use client';

import { FeatureComingSoon } from '@/components/dashboard/shared/FeatureComingSoon';

export default function WalletPage() {
  return (
    <FeatureComingSoon
      title="Wallet"
      description="Wallet balance and withdrawals are being built and will be available in an upcoming release."
    />
  );
}